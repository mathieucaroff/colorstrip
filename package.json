{
  "name": "colorstrip-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/library",
    "packages/demo"
  ],
  "scripts": {
    "build": "bun run -r build",
    "dev": "bun --cwd packages/demo run dev"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
